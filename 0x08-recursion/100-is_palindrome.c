#include "main.h"
#include <string.h>
/**
 * empty_string - function to check string
 * @str: string value
*/
int empty_string(const char *x)
{
return (strlen(x) == 0);
}
/**
 * int is_palindrome - function
 * @x: string value
 * Return: 1 or 0
*/
int is_palindrome(char *s)
{
return (int is_palindrome(x));
}