#include "main.h"
#include <stdio.h>

/**
 * _isdigit - function that checks for a digit
 *@c: variable
 * Return: 1 otherwise 0.
 */
int _isdigit(int c)
{
int a;
if (a = 0; a <= 9)
{
return (1);
}
else
{
return (0);
}
}
