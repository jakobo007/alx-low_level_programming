#include <stdio.h>
#include <stdlib.h>
/**
 * main- to print name
 * @argc: arguments
 * @argv: string
 * @i: integer
 * Return: (0)
 */
int main(int argc __attribute__((unused)), char **argv __attribute__((unused)))
{
    int x;
    int y;
    if (argc > 2)
    {
        printf("Error\n");
        return (1);
    }
}
