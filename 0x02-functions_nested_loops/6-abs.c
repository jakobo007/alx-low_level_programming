#include "main.h"
#include <stdio.h>
/**
 * _abs - check the code for absolute value
 *
 * @a parameter to be checked
 * 
 * Return: Always a.
 */
int _abs(int)
{
if ( a < 0)
a = -(a);
else if (a >= 0)
a = a;
}
